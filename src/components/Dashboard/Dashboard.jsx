// eslint-disable-next-line no-unused-vars
import BillList from "../Bill/BillList";

const Dashboard = () => {
    return (
        <div>
            <h1>Dashboard</h1>
        </div>
    );
}

export default Dashboard;
